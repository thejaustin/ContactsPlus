<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/activity_margin">

    <TextView
        android:id="@+id/import_social_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Import social links from Snapchat or Facebook backup (JSON)."
        android:layout_marginBottom="@dimen/activity_margin" />

    <Button
        android:id="@+id/import_social_select_file"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select JSON File" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/import_social_list"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        android:layout_marginTop="@dimen/activity_margin"
        android:visibility="gone" />

</LinearLayout>
